/**
 * Conținutul `data` pentru logout / confirm email / reset password.
 * Controller returnează: { message }
 */
export class MessageDataDto {
  /**
   * Mesaj descriptiv pentru client
   * @example Operația a fost finalizată cu succes.
   */
  message: string;
}
